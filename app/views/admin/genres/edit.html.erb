<div="row">
  <h5 class = p-3 >ジャンル編集</h5>
</div>
<div class="row">
  <%= form_with model: @genre,method: :patch, url:admin_genre_path do |f| %>

  <%= f.label :ジャンル名, class:"p-3" %>

  <%= f.text_field :name, placeholder: "ジャンル名", class:"mr-5" %>
 <%= f.submit "変更を保存", class: "btn btn-success" %>

  <% end %>
</div>